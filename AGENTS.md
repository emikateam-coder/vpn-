# AGENTS.md

## Cursor Cloud specific instructions

This repository (`vpn-`) is currently an empty/stub project with only a `README.md` file. There is no application code, no dependencies, no build system, and no services to run.

### Current state

- Single file: `README.md` (contains only `# vpn-`)
- Single commit on `main` branch
- No package manager files (`package.json`, `requirements.txt`, `go.mod`, etc.)
- No Docker or container configuration
- No CI/CD pipeline
- No tests, linting, or build configuration

### For future agents

Once code is added to this repository, this section should be updated with:
- How to install dependencies
- How to run the application in development mode
- How to run tests and linting
- Any non-obvious setup caveats
